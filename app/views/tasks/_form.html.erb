<%= simple_form_for(task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <% unless task.new_record? %>
    <%= f.input :completed %>
  <% end %>
  <%= f.button :submit, class: 'btn btn-primary mb-3' %>
<% end %>
